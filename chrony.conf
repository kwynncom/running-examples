server kwynn.com iburst minpoll 0 maxpoll 8 xleave prefer
# server time.nist.gov minpoll 5 maxpoll 10 xleave

allow

log rawmeasurements measurements statistics tracking rtc refclocks tempcomp
logbanner 24

keyfile /etc/chrony/chrony.keys
driftfile /var/lib/chrony/chrony.drift
logdir /var/log/chrony
maxupdateskew 100.0
rtcsync

# originally had iburst before maxsources
# pool ntp.ubuntu.com        maxsources 4
# pool 0.ubuntu.pool.ntp.org maxsources 1
# pool 1.ubuntu.pool.ntp.org maxsources 1
# pool 2.ubuntu.pool.ntp.org maxsources 2

# Kwynn - see SHM 0 / refclock for GPS in saved versions

